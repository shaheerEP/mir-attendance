
import { loadModels } from '../src/lib/face-recognition';

console.log('Import successful.');
try {
    // Just checking if code runs without syntax error
    console.log('Code valid.');
} catch (e) {
    console.error(e);
}
